import * as types from './types'

export const SET_ACTIVE_LOGO = { type: types.SET_ACTIVE_LOGO }
export const SET_LOGOS = { type: types.SET_LOGOS }
export const START_GAME = { type: types.START_GAME }
export const CORRECT_GUESS = { type: types.CORRECT_GUESS }
export const INCORRECT_GUESS = { type: types.INCORRECT_GUESS }
export const RESET_GAME = { type: types.RESET_GAME }
export const GAME_OVER = { type: types.GAME_OVER }
